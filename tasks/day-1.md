# **День 1: Первый бот за 2 часа — от нуля до команд**  
*Руководство по шагам, как на реальном занятии*

---

## 📁 Шаг 1. Создание проекта

Создайте папку и откройте `pytelegrambot` в `Visual Studio Code`.

Теперь вы находитесь в папке `pytelegrambot`.

---

## 🔐 Шаг 2. Файл `.env` — хранилище токена

Создайте файл `.env` (без расширения!) и добавьте в него:

```env
BOT_TOKEN=
```

> 💡 Позже вы вставите сюда токен от @BotFather. Сейчас оставьте его пустым — это поможет нам увидеть, как выглядит ошибка при отсутствии токена.

---

## 🧪 Шаг 3. Виртуальное окружение

Выполните в PowerShell:

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
```

После этого в начале строки появится `(venv)`, например:

```powershell
(venv) PS C:\Users\nurzhanov_o\Documents\pytelegrambot>
```

Это значит — вы внутри изолированной среды Python.

---

## 📦 Шаг 4. Установка зависимостей через `requirements.txt`

Создайте файл `requirements.txt` со следующим содержимым:

```txt
dotenv
pyTelegramBotAPI
```

> ⚠️ Обратите внимание: правильное название пакета — **`dotenv`**, а не просто `dotenv`.

Установите зависимости:

```powershell
pip install -r requirements.txt
```

---

## 🧩 Шаг 5. Базовый файл `main.py` — загрузка токена

Создайте файл `main.py` и вставьте:

```python
import os
from dotenv import load_dotenv
import telebot

# Загружаем переменные окружения
load_dotenv()

# Получаем токен
TELEGRAM_TOKEN = os.environ.get('BOT_TOKEN')

print(TELEGRAM_TOKEN)
```

Запустите:

```powershell
python main.py
```

**Результат:**  
В терминале вы увидите `None`, потому что в `.env` токен пока не указан.

---

## ❌ Шаг 6. Симуляция ошибки — защита от пустого токена

Измените код в `main.py`:

```python
import os
from dotenv import load_dotenv
import telebot

load_dotenv()

TELEGRAM_TOKEN = os.environ.get('BOT_TOKEN')
if not TELEGRAM_TOKEN:
    raise ValueError("❌ Токен не найден! Проверьте файл .env")
```

Запустите снова:

```powershell
python main.py
```

**Ожидаемая ошибка:**
```
ValueError: ❌ Токен не найден! Проверьте файл .env
```

> ✅ Это полезно! Теперь программа сама подскажет, если вы забыли токен.

---

## 🔑 Шаг 7. Добавляем настоящий токен

1. Откройте Telegram → найдите **@BotFather**.
2. Создайте бота через `/newbot`.
3. Скопируйте токен (например: `7047199432:AAFAQ3J7TA432235AjHh36043C54r2II4`).
4. Вставьте его в `.env`:

```env
BOT_TOKEN=здесь_вставьте_токен
```

> 🔒 Никогда не коммитьте `.env` в Git!

Проверьте, что токен загружается:

```python
# main.py (временно)
load_dotenv()
TELEGRAM_TOKEN = os.environ.get('BOT_TOKEN')
if not TELEGRAM_TOKEN:
    raise ValueError("❌ Токен не найден! Проверьте файл .env")
print("✅ Токен загружен:", TELEGRAM_TOKEN[:10] + "...")  # покажем только начало
```

Запустите — должно быть:
```
✅ Токен загружен: 7047199392...
```

---

## 🤖 Шаг 8. Создаём объект бота

Добавьте в `main.py`:

```python
bot = telebot.TeleBot(TELEGRAM_TOKEN)
```

---

## 👋 Шаг 9. Первая команда — `/start`

Добавьте обработчик:

```python
@bot.message_handler(commands=['start'])
def send_welcome(message):
    name = message.from_user.first_name
    bot.reply_to(message, f"Привет, {name}! 👋 Я твой первый Telegram-бот.")
```

---

## ▶️ Шаг 10. Запуск бота

Добавьте в конец файла:

```python
if __name__ == '__main__':
    print("✅ Бот запущен! Нажмите Ctrl+C, чтобы остановить.")
    bot.infinity_polling()
```

Запустите:

```powershell
python main.py
```

**Вывод:**
```
✅ Бот запущен! Нажмите Ctrl+C, чтобы остановить.
```

Теперь откройте Telegram, найдите своего бота и напишите `/start`.  
Бот должен ответить: «Привет, [Ваше имя]! 👋 Я твой первый Telegram-бот.»

---

## ℹ️ Шаг 11. Добавляем `/help`

Добавьте новый обработчик:

```python
@bot.message_handler(commands=['help'])
def send_help(message):
    help_text = (
        "Я умею:\n"
        "/start — поздороваться\n"
        "/help — показать эту справку\n"
        "Напиши что-нибудь — я повторю!"
    )
    bot.reply_to(message, help_text)
```

Перезапустите бота (`Ctrl+C`, затем `python main.py`) и проверьте `/help`.

---

## 🎯 Практика: команда `/caps`

### Версия 1 (простая, но не совсем правильная)

```python
@bot.message_handler(commands=['caps'])
def send_caps(message):
    msg = message.text  # Например: "/caps привет"
    bot.reply_to(message, msg.upper())
```

**Проблема:** Бот вернёт `/CAPS ПРИВЕТ`, а нужно только `ПРИВЕТ`.

---

### Версия 2 (правильная)

```python
@bot.message_handler(commands=['caps'])
def send_caps(message):
    msg = message.text[6:].strip()  # Пропускаем "/caps " (6 символов)
    bot.reply_to(message, msg.upper())
```

> 💡 Почему `6`? Потому что `/caps` — 5 символов + пробел = 6.

**Пример:**  
Вы пишете: `/caps hello world`  
Бот отвечает: `HELLO WORLD`

---

## 💬 Шаг 12. Эхо-режим — обработка любого текста

Добавьте универсальный обработчик:

```python
@bot.message_handler(func=lambda message: True)
def echo_all(message):
    user_message = message.text.strip()
    if 'привет' == user_message.lower():
        bot.reply_to(message, "Привет! Рад тебя видеть! Чем могу помочь?\n/help — показать эту справку")
    else:
        bot.reply_to(message, user_message)
```

> 📝 `func=lambda message: True` означает: «обрабатывать **все** сообщения, которые не попали под другие команды».

---

## 🧪 Итоговая структура `main.py`

```python
import os
from dotenv import load_dotenv
import telebot

# Загружаем переменные окружения
load_dotenv()

# Получаем токен
TELEGRAM_TOKEN = os.environ.get('BOT_TOKEN')
if not TELEGRAM_TOKEN:
    raise ValueError("❌ Токен не найден! Проверьте файл .env")

# Создаём объект бота
bot = telebot.TeleBot(TELEGRAM_TOKEN)

# /start
@bot.message_handler(commands=['start'])
def send_welcome(message):
    name = message.from_user.first_name
    bot.reply_to(message, f"Привет, {name}! 👋 Я твой первый Telegram-бот.")

# /help
@bot.message_handler(commands=['help'])
def send_help(message):
    help_text = (
        "Я умею:\n"
        "/start — поздороваться\n"
        "/help — показать эту справку\n"
        "/caps <текст> — преобразовать в ЗАГЛАВНЫЕ\n"
        "Напиши что-нибудь — я повторю!"
    )
    bot.reply_to(message, help_text)

# /caps
@bot.message_handler(commands=['caps'])
def send_caps(message):
    msg = message.text[6:].strip()
    bot.reply_to(message, msg.upper())

# Любой текст
@bot.message_handler(func=lambda message: True)
def echo_all(message):
    user_message = message.text.strip()
    if 'привет' == user_message.lower():
        bot.reply_to(message, "Привет! Рад тебя видеть! Чем могу помочь?\n/help — показать эту справку")
    else:
        bot.reply_to(message, user_message)

# Запуск
if __name__ == '__main__':
    print("✅ Бот запущен! Нажмите Ctrl+C, чтобы остановить.")
    bot.infinity_polling()
```

---

## ✅ Что вы сделали сегодня?

- Настроили проект с виртуальным окружением  
- Безопасно хранили токен в `.env`  
- Обрабатывали команды `/start`, `/help`, `/caps`  
- Реализовали эхо-режим с условием на «привет»  
- Увидели, как выглядит ошибка при отсутствии токена  
- Запустили настоящего работающего Telegram-бота!

---

## 📚 Домашнее задание (повторение)

1. Убедитесь, что бот работает корректно.  
2. Попробуйте изменить приветствие — например, добавить эмодзи или возраст.  
3. **Дополнительно:** сделайте так, чтобы команда `/caps` работала даже если после неё ничего нет (выводить подсказку).

---

Завтра: кнопки, заметки и медиа! 🎮📸  
А пока — поздравляю: вы стали разработчиком Telegram-ботов! 🎉