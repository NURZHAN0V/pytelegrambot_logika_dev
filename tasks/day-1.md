# День 1: Первый бот за 2 часа — от нуля до команд

Инструкция по запуску программы  
*(для абсолютных новичков — шаг за шагом)*

---

### 1. Создание виртуального окружения  
Команда создаёт изолированную среду для проекта, чтобы избежать конфликтов зависимостей с другими Python‑проектами.

```powershell
python -m venv venv
```

**Результат:**  
В директории проекта появится папка `venv` со всеми необходимыми файлами окружения.

> 💡 **Совет:** Если команда не работает, убедитесь, что у вас установлен Python 3.7 или новее. Проверить можно командой `python --version`.

---

### 2. Разрешение выполнения скриптов в PowerShell (Windows)  
Эта команда требуется только один раз после создания окружения — она разрешает запуск скриптов в текущем пользовательском контексте.

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Важно:**  
- Выполняется в **PowerShell** (не в CMD).  
- Система может запросить подтверждение — введите `Y` и нажмите **Enter**.  
- Эта настройка безопасна: она разрешает запуск только локальных или подписанных скриптов.

---

### 3. Активация виртуального окружения  
Активирует изолированную среду, чтобы все последующие команды `pip` и `python` использовали её зависимости.

```powershell
venv\Scripts\Activate.ps1
```

**Признаки успеха:**  
- В терминале появится префикс `(venv)` перед путём.  
  Пример: `(venv) C:\Users\Oleg\pytelegrambot>`  
- Все установки пакетов через `pip` будут применяться **только к этому окружению**.

> ⚠️ Если вы видите ошибку «cannot be loaded because running scripts is disabled», вернитесь к шагу 2 и убедитесь, что политика выполнения изменена.

---

### 4. Установка необходимых библиотек  
Теперь установим две библиотеки, без которых бот не заработает:

```powershell
pip install pyTelegramBotAPI python-dotenv
```

**Что делает каждая?**  
- `pyTelegramBotAPI` — основная библиотека для работы с Telegram (часто называют `telebot`).  
- `python-dotenv` — позволяет хранить секреты (например, токен бота) в отдельном файле `.env`, а не в коде.

**Проверка установки:**  
После завершения вы увидите сообщение вроде `Successfully installed ...`. Это значит — всё готово!

---

### 5. Создание структуры проекта  

В папке `pytelegrambot` создайте два файла:

#### Файл `.env`  
(без расширения! просто `.env`)

```env
BOT_TOKEN=ваш_токен_здесь
```

> 📌 **Как получить токен?**  
> 1. Откройте Telegram.  
> 2. Найдите бота **@BotFather**.  
> 3. Напишите `/start`, затем `/newbot`.  
> 4. Следуйте инструкциям: придумайте имя (например, `MyFirstTestBot`) и username (должен оканчиваться на `bot`, например `my_first_test_bot`).  
> 5. BotFather пришлёт строку вида:  
>    `Use this token to access the HTTP API: 1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`  
> 6. Скопируйте **всю строку после двоеточия** и вставьте вместо `ваш_токен_здесь`.

> 🔒 **Никогда не публикуйте токен!** Он даёт полный доступ к вашему боту.

#### Файл `main.py`

Откройте любой текстовый редактор (лучше VS Code, но подойдёт и Блокнот). Вставьте следующий код:

```python
# main.py
import os
from dotenv import load_dotenv
import telebot

# Загружаем переменные из .env
load_dotenv()

# Получаем токен
TOKEN = os.getenv("BOT_TOKEN")
if not TOKEN:
    raise ValueError("❌ Токен не найден! Проверьте файл .env")

# Создаём объект бота
bot = telebot.TeleBot(TOKEN)

# Обработчик команды /start
@bot.message_handler(commands=['start'])
def send_welcome(message):
    name = message.from_user.first_name
    bot.reply_to(message, f"Привет, {name}! 👋 Я твой первый Telegram-бот.")

# Обработчик команды /help
@bot.message_handler(commands=['help'])
def send_help(message):
    help_text = (
        "Я умею:\n"
        "/start — поздороваться\n"
        "/help — показать эту справку\n"
        "Напиши что-нибудь — я повторю!"
    )
    bot.reply_to(message, help_text)

# Обработчик любого текста
@bot.message_handler(func=lambda m: True)
def echo_all(message):
    text = message.text.lower()
    if "привет" in text:
        bot.reply_to(message, "И тебе привет! 😊")
    else:
        bot.reply_to(message, f"Ты написал: {message.text}")

# Запуск бота
if __name__ == '__main__':
    print("✅ Бот запущен! Нажмите Ctrl+C, чтобы остановить.")
    bot.infinity_polling()
```

> 🧠 **Объяснение для новичков:**  
> - `@bot.message_handler(...)` — это «декоратор». Он говорит: «Когда придёт такая команда — выполни эту функцию».  
> - `bot.reply_to(...)` — отправляет ответ **в виде ответа на сообщение** (красиво и понятно).  
> - `bot.infinity_polling()` — заставляет бота постоянно «слушать» Telegram.

---

### 6. Запуск приложения  

```powershell
python main.py
```

**Ожидаемое поведение:**  
- В терминале появится надпись: `✅ Бот запущен! Нажмите Ctrl+C, чтобы остановить.`  
- Бот **уже работает**! Откройте Telegram, найдите своего бота (по имени, которое вы задали в @BotFather) и напишите ему `/start`.

**Проверка функций:**  
| Действие | Ожидаемый результат |
|--------|------------------|
| `/start` | Бот здоровается по имени |
| `/help` | Появляется список команд |
| `Привет!` | Бот отвечает: «И тебе привет! 😊» |
| `Как дела?` | Бот отвечает: «Ты написал: Как дела?» |

> ⏱️ **Важно:** Первый запуск может занять 10–20 секунд — Telegram иногда медленно доставляет первые сообщения.

---

### 7. Остановка бота  
Чтобы остановить программу, нажмите в терминале:  
**`Ctrl + C`** (удерживайте обе клавиши одновременно).

Вы увидите сообщение вроде `KeyboardInterrupt`, и программа завершится.

---

### Домашнее задание (обязательное)

**Задача:** Добавить команду `/caps <текст>`, которая превращает текст в заглавные буквы.

**Пример:**  
Вы пишете: `/caps привет, мир!`  
Бот отвечает: `ПРИВЕТ, МИР!`

**Подсказки:**  
1. Создайте новый обработчик:  
   ```python
   @bot.message_handler(commands=['caps'])
   ```
2. Извлеките текст после команды:  
   ```python
   parts = message.text.split(maxsplit=1)
   if len(parts) < 2:
       bot.reply_to(message, "Напиши текст после /caps!")
       return
   text_to_capitalize = parts[1]
   ```
3. Преобразуйте в заглавные: `text_to_capitalize.upper()`
4. Отправьте результат.

**Готовый фрагмент (если застряли):**
```python
@bot.message_handler(commands=['caps'])
def caps_command(message):
    parts = message.text.split(maxsplit=1)
    if len(parts) < 2:
        bot.reply_to(message, "Пожалуйста, напиши текст после /caps. Например: /caps привет")
        return
    result = parts[1].upper()
    bot.reply_to(message, result)
```

> ✅ После добавления перезапустите бот (`Ctrl+C`, затем `python main.py`) и протестируйте!

---

### Что дальше?

Завтра мы научим бота:
- Показывать кнопки 🎮  
- Запоминать ваши заметки 📝  
- Принимать фото 📸  

А пока — похвалите себя! Вы создали настоящего Telegram-бота с нуля. 🎉