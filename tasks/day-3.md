# День 3: Инлайн-кнопки, голосовые и стикеры
*Делаем бота живее: кнопки под сообщениями, приём голоса и реакция на стикеры*

---

Перед началом убедитесь, что вы прошли **День 1** и **День 2** ([day-1.md](day-1.md), [day-2.md](day-2.md)): у вас есть бот с командами, обычными кнопками, фото, видео и заметками в папках. Сегодня мы добавим **инлайн-кнопки** (они появляются прямо под сообщением и не занимают место внизу экрана), научим бота принимать **голосовые сообщения** и весело реагировать на **стикеры**. Всё это сделает бота интереснее и удобнее в телефоне.

---

## 🔘 Шаг 1. Чем инлайн-кнопки отличаются от обычных

В Дне 1 мы делали кнопки внизу экрана («О нас», «Отправить фото», «Мои заметки») — это **обычная клавиатура** (`ReplyKeyboardMarkup`). Она заменяет клавиатуру телефона и всегда видна.

**Инлайн-кнопки** — это кнопки **под конкретным сообщением**. Они не занимают место внизу и исчезают, когда сообщение далеко в переписке. Идеально для выбора «Да / Нет», «Помощь», «Подсказка» и т.п.

Сегодня мы добавим сообщение с такими кнопками и научим бота реагировать на нажатие.

---

## 📩 Шаг 2. Команда `/menu` с кнопками под сообщением

Создадим команду `/menu`: бот отправит одно сообщение, а под ним — несколько кнопок. Пользователь нажимает кнопку — бот узнаёт, какую именно нажали, и может ответить по-разному.

Добавьте **функцию**, которая создаёт кнопки (можно разместить рядом с `show_main_menu` или перед обработчиками):

```python
def show_inline_menu(chat_id):
    markup = types.InlineKeyboardMarkup(row_width=2)
    btn_help = types.InlineKeyboardButton("📖 Помощь", callback_data="menu_help")
    btn_about = types.InlineKeyboardButton("🤖 О боте", callback_data="menu_about")
    btn_tip = types.InlineKeyboardButton("💡 Подсказка", callback_data="menu_tip")
    markup.add(btn_help, btn_about, btn_tip)
    bot.send_message(
        chat_id,
        "Выберите действие:",
        reply_markup=markup
    )
```

> 💡 `callback_data` — это короткая «метка» кнопки. Когда пользователь нажмёт кнопку, бот получит именно эту строку (например, `"menu_help"`) и по ней поймёт, что нажали. Имена придумываем мы сами, главное — без пробелов и не слишком длинные.

Добавьте **обработчик команды** `/menu` (например, после обработчика `/help`):

```python
@bot.message_handler(commands=['menu'])
def send_menu(message):
    show_inline_menu(message.chat.id)
```

Перезапустите бота и напишите `/menu`. Под сообщением «Выберите действие:» должны появиться три кнопки. Пока при нажатии ничего не будет — это мы сделаем в следующем шаге.

---

## 👆 Шаг 3. Реакция на нажатие инлайн-кнопки

Когда пользователь нажимает инлайн-кнопку, Telegram присылает боту **callback** (отклик) с данными кнопки. Обрабатываем его отдельным обработчиком — `callback_query_handler`.

Добавьте **обработчик нажатий** (лучше разместить его **до** универсального обработчика с `func=lambda message: True`, чтобы не путать с обычными сообщениями):

```python
@bot.callback_query_handler(func=lambda call: True)
def handle_menu_buttons(call):
    if call.data == "menu_help":
        bot.answer_callback_query(call.id, "Сейчас покажу справку!")
        bot.send_message(call.message.chat.id, "Команды: /start, /help, /caps, /note, /notes, /save, /menu. Фото и видео тоже принимаю!")
    elif call.data == "menu_about":
        bot.answer_callback_query(call.id)
        bot.send_message(
            call.message.chat.id,
            "🤖 Я — учебный бот. Создан на занятиях по Python и Telegram!"
        )
    elif call.data == "menu_tip":
        bot.answer_callback_query(call.id, "Подсказка: нажми /help для списка команд")
```

> 💡 `bot.answer_callback_query(call.id)` — обязательный вызов: он убирает «часики» на кнопке и говорит Telegram, что мы обработали нажатие. Можно передать второй аргумент — короткий текст-подсказку, который пользователь увидит вверху экрана (например: «Сейчас покажу справку!»).

Перезапустите бота, нажмите `/menu` и поочерёдно нажмите каждую кнопку. Бот должен отвечать разным текстом на каждую.

---

## 📐 Шаг 4. Ещё кнопки: в ряд и столбиком

Кнопки можно раскладывать по-разному: в одну линию, в два столбца, или по одной в столбик.

- **В один ряд:** добавьте несколько кнопок в одном `markup.add(btn1, btn2, btn3)` — они встанут в линию (если помещаются).
- **В столбик:** добавляйте по одной: `markup.add(btn1)`, `markup.add(btn2)` — каждая кнопка с новой строки.
- **Два в ряд, остальные ниже:** `row_width=2` и потом `markup.add(btn1, btn2, btn3)` — первые две в ряд, третья под ними.

Попробуйте добавить в меню четвёртую кнопку, например «🎲 Случайное число», с `callback_data="menu_random"`. В обработчике для `"menu_random"` отправьте сообщение с числом:

```python
import random
# в начале файла с импортами

# в show_inline_menu добавьте кнопку:
btn_random = types.InlineKeyboardButton("🎲 Случайное число", callback_data="menu_random")
markup.add(btn_random)

# в handle_menu_buttons добавьте:
elif call.data == "menu_random":
    num = random.randint(1, 100)
    bot.answer_callback_query(call.id, f"Выпало: {num}")
    bot.send_message(call.message.chat.id, f"🎲 Случайное число от 1 до 100: {num}")
```

> 💡 Модуль `random` встроен в Python — импортировать достаточно один раз в начале файла. `randint(1, 100)` даёт целое число от 1 до 100 включительно.

Проверьте: `/menu` → «Случайное число» — каждый раз должно появляться новое число.

---

## 🎤 Шаг 5. Голосовые сообщения

В Telegram можно отправлять голосовые сообщения (как в мессенджере). Бот может их принимать и, например, сохранять в папку или просто отвечать текстом.

Создайте папку для голосовых при старте (рядом с `images`, `videos`, `notes`):

```python
os.makedirs("voices", exist_ok=True)
```

Добавьте **обработчик голосовых сообщений** (можно после обработчика видео):

```python
@bot.message_handler(content_types=['voice'])
def handle_voice(message):
    file_id = message.voice.file_id
    file_info = bot.get_file(file_id)
    downloaded = bot.download_file(file_info.file_path)
    file_path = f"voices/{file_id}.ogg"
    with open(file_path, 'wb') as f:
        f.write(downloaded)
    bot.reply_to(message, "🎤 Голосовое сохранено! Спасибо.")
```

> 💡 У голосовых в Telegram расширение обычно `.ogg`. Объект сообщения — `message.voice` (как `message.video` у видео).

Перезапустите бота и отправьте ему голосовое сообщение. Бот должен ответить и сохранить файл в папку `voices/`.

---

## 🎭 Шаг 6. Стикеры — весёлая реакция

Стикеры в Telegram — это картинки с эмоциями. Бот может их принимать и реагировать: например, отправить свой стикер или короткий текст.

Добавьте **обработчик стикеров** (можно перед универсальным обработчиком текста):

```python
@bot.message_handler(content_types=['sticker'])
def handle_sticker(message):
    bot.reply_to(message, "😄 Классный стикер! Отправь ещё или напиши /menu.")
```

> 💡 У стикера есть `message.sticker.file_id` — по нему можно при желании отправить тот же стикер обратно: `bot.send_sticker(message.chat.id, message.sticker.file_id)`. Пока мы просто отвечаем текстом.

Перезапустите бота и отправьте любой стикер. Бот должен ответить фразой про стикер.

---

## 🔗 Шаг 7. Упоминание `/menu` в приветствии

Чтобы пользователи узнали про новое меню, можно в приветствии по `/start` или в `/help` добавить строчку про `/menu`.

Например, в обработчике `/start` после приветствия и обычного меню вызовите отправку инлайн-меню:

```python
@bot.message_handler(commands=['start'])
def send_welcome(message):
    name = message.from_user.first_name
    bot.reply_to(message, f"Привет, {name}! 👋 Я твой первый Telegram-бот.")
    show_main_menu(message)
    show_inline_menu(message.chat.id)  # добавили: сразу показываем кнопки под сообщением
```

Либо просто добавьте в текст `/help` строку:  
`/menu — показать меню с кнопками под сообщением`

Перезапустите бота и проверьте: после `/start` должны появиться и обычные кнопки внизу, и сообщение с инлайн-кнопками.

---

## ✅ Что вы сделали сегодня?

- Узнали, чем инлайн-кнопки отличаются от обычных (кнопки под сообщением, не внизу экрана).
- Добавили команду `/menu` с кнопками «Помощь», «О боте», «Подсказка» (и по желанию «Случайное число»).
- Настроили обработку нажатий через `callback_query_handler` и `callback_data`.
- Научились раскладывать кнопки в ряд и столбиком (`row_width`, несколько `add`).
- Добавили приём голосовых сообщений и сохранение в папку `voices/`.
- Добавили реакцию на стикеры.
- Подключили инлайн-меню к приветствию или к справке `/help`.

---

## 📚 Домашнее задание

1. Убедитесь, что `/menu` и все инлайн-кнопки работают, голосовые сохраняются в `voices/`, на стикеры бот отвечает. Перезапустите бота и проверьте по очереди.
2. Добавьте в `/help` описание команд `/menu`, а также то, что бот принимает голосовые и стикеры.
3. **Дополнительно:** добавьте инлайн-кнопку «Повторить случайное число» с тем же `callback_data="menu_random"` — при нажатии снова отправлять новое число.
4. **Дополнительно:** при нажатии на «О боте» не только отправляйте текст, но и редактируйте само сообщение с кнопками: `bot.edit_message_text("Вы нажали: О боте", call.message.chat.id, call.message.message_id)` — тогда кнопки исчезнут и останется только обновлённый текст.

---

День 4: напоминания, таймеры и простые сценарии! ⏰  
Продолжайте в том же духе — вы уже делаете по-настоящему живого бота! 🎉
